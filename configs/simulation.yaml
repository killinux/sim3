simulation:
  num_agents: 100
  num_simulated_days: 1
  random_seed: 42

agent:
  max_sessions_per_day: 3
  max_videos_per_session: 25
  memory_window: 30

llm:
  provider: deepseek
  model: deepseek-chat
  temperature: 0.7
  max_tokens: 300
  max_concurrent: 32
  timeout_seconds: 30

recommendation:
  top_k: 10
  epsilon: 0.1
  diversity_weight: 0.2

ab_test:
  enabled: false
  treatment_ratio: 0.5
  treatment_config:
    diversity_weight: 0.4

content_pool:
  num_videos: 5000
  num_categories: 20
